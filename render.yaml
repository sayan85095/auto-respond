services:
  - type: web
    name: whatsapp-auto-agent
    env: docker
    plan: free
    region: singapore
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: wwebjs_session
      mountPath: /usr/src/app/.wwebjs_auth
      sizeGB: 1
